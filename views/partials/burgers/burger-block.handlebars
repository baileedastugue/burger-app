{{!-- <ul> --}}
    <button data-id="{{this.id}}" class="change-devour btn btn-warning" data-newdevour="{{devour}}">
        {{#if devour}}
            Devour
            {{else}}
            Remake!
        {{/if}}
    </button>  <button data-id="{{this.id}}" class="delete btn btn-danger">Delete</button><br>

{{!-- </ul> --}}